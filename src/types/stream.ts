export interface Stream {
    id: string;
    title: string;
    creatorName: string;
    collaborators: string;
    scheduledTime: Date;
    imageUrl: string | null;
    description?: string;
  }
  
